fastapi==0.115.6
uvicorn[standard]==0.34.0
anthropic==0.105.2
sayari==0.1.43
neo4j==5.27.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
sse-starlette==2.2.1
upstash-redis==1.2.0
python-dotenv==1.0.1
# Phase D (IMS): L2 episodic vector memory in Upstash Vector, behind
# EPISODIC_MEMORY_ENABLED. Uses Upstash's HOSTED embedding model (raw-text
# upsert/query), so no separate embedding provider/key is needed. A graceful
# no-op when the vector env vars / flag are absent — the live demo is unaffected.
upstash-vector==0.8.0
# Phase 5: LangGraph agent implementation (behind AGENT_IMPL=graph) + LangSmith.
langgraph==1.2.2
langchain-anthropic==1.4.4
langchain-core==1.4.0
langsmith==0.8.6
